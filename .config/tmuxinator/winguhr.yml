# ~/.config/tmuxinator/winguhr.yml

name: winguhr
root: ~/

windows:
  - api:
      root: $API
      panes:
        - cd $API && nvim .

  - frontend:
      root: $FRONTEND
      panes:
        - cd $FRONTEND && nvim .

  - processes:
      layout: even-horizontal
      panes:
        - cd $API && composer dev
        - cd $FRONTEND && npm run dev
